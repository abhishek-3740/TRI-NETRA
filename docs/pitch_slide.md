# Pitch Slide — Air-Gapped Forensic Integrity

**Headline:** "TRINETRA reconstructs the exact millisecond a suspect called, logged in, and moved money — 100% locally, with zero data ever leaving the police network."

**Supporting points:**
- Hybrid parsing: native text extraction + CPU-optimized OCR, no cloud LLMs.
- CGNAT-defeat via NAT-port resolution links masked IPDR sessions to UPI narrations.
- Neo4j-backed graph analysis (bounded-depth cycle detection) finds smurfing rings in milliseconds.
- ST-DBSCAN proves physical suspect co-location from CDR alone.
- ML progression: rule-based scoring -> Isolation Forest -> GraphSAGE/Node2Vec graph embeddings.
