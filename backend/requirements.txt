# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4

# --- Data processing ---
polars==0.20.31
pandas==2.2.2
numpy==1.26.4

# --- PDF / OCR parsing ---
pdfplumber==0.11.1
# paddleocr / paddlepaddle are heavy CPU installs — uncomment when ready for Tier 2 OCR
# paddleocr==2.7.3
# paddlepaddle==2.6.1

# --- Fuzzy matching ---
rapidfuzz==3.9.3

# --- Databases ---
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
alembic==1.13.1
neo4j==5.22.0

# --- Graph & spatial ---
networkx==3.3
scikit-learn==1.5.0
# node2vec runs on plain NetworkX graphs — lightweight, no C++ toolchain needed
node2vec==0.4.6

# --- GraphSAGE stack (primary ML goal; see Day 11 safety-valve checkpoint in docs) ---
# torch / torch-geometric can be finicky on CPU-only boxes. Install separately if needed:
# torch==2.3.1
# torch-geometric==2.5.3

# --- Reporting ---
reportlab==4.2.2
python-docx==1.1.2

# --- Testing ---
pytest==8.2.2
httpx==0.27.0
